// src/config/swagger.ts
// Import the auto-generated swagger file
// Run "npm run swagger" to regenerate after adding new routes

import swaggerOutput from "../docs/swagger-output.json";

export const swaggerSpec = swaggerOutput;
